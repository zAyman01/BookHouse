# models/

# - Mongoose schema and model definitions

# - Define the shape of data stored in MongoDB

# Example files:

# User.js → name, email, password, role, createdAt

# Book.js → title, author, price, category, stock, cover

# Order.js → user, items, total, status, createdAt

# Review.js → user, book, rating, comment
