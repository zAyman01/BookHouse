# middleware/

# - Functions that run between the request and the controller

# Example files:

# protect.js → Verify JWT and attach req.user

# authorize.js → Role-based access control (admin, user)

# validate.js → Run validation schema and return 400 on failure

# errorHandler.js → Global error handler (last middleware in chain)

# notFound.js → 404 handler for undefined routes
